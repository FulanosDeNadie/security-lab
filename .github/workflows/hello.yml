name: Trigger experiment

on:
  push:
  pull_request: 
    types: [opened]
    
jobs:
  trigger-test:
    runs-on: ubuntu-latest

    steps:
      - name: Show message
        run: echo "The workflow was triggered by a pull request"