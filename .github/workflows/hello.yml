name: Hello Workflow

on:
  push:

jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "Hello GitHub Actions"
        
      - name: Break things
        run: comando-que-no-existe

      - name: Say goodbye
        run: echo "Goodbye GitHub Actions"